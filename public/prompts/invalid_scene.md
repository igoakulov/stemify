# INVALID SCENE

Your last response with scene code had validation errors:

{{errors}}

Fix these issues and respond with a corrected JSON object containing only the `scene` field:

```json
{
  "scene": {
    "sceneCode": "..."
  }
}
```

**Do not** include any additional text, explanations, or markdown formatting outside the JSON block.
