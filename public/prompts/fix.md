# FIX

Your previous response had validation errors.

## Errors

{{errors}}

## Invalid JSON

{{invalid_json}}

Fix these issues and respond with a corrected JSON object:

```json
{
  "scene": "scene.addCircle({ id: \"circle\", radius: 1 });"
}
```

Then write any explanation as markdown below the code block.

The JSON code block must contain ONLY the JSON object.
You may add markdown text for the user, but only before or after the JSON block - NOT inside.

See ## Primitives, ## Complex Shapes & Compositions, and ## Infrastructure for valid methods and attributes.
